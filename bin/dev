#!/usr/bin/env bash

bundle check || bundle install

set -e
bin/qr-code

bundle exec jekyll serve \
                          --drafts \
                          --unpublished \
                          --livereload \
                          --watch \
                          --host "$IP_ADDR" \
                          --port "$PORT" \
                          --open
