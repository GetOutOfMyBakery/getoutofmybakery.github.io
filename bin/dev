#!/usr/bin/env bash

bundle check || bundle install
bundle exec jekyll serve \
                          --drafts \
                          --unpublished \
                          --livereload \
                          --watch \
                          --open
